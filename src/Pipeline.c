#include<Pipeline.h>
/*
    Each node will both be client and server
    The only data exchange pipeline is here
*/
extern
byte linked_PourData(byte* buf, int len){
    return 0;
}
extern 
byte linked_PourByte(byte buf){
    return 0;
}